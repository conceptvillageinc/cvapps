# CV見積アプリ v2 ロードマップ（案件〜請求〜管理表）

2026-09-15 にいただいた全体像（PDF 6ページ）を、既存のデータ構造と照らし合わせて
整理したもの。※1〜※12 の相談事項への提案も含む。

---

## 1. 全体像の要約

現在のアプリは「クライアント → 見積」の2階層。これを

```
クライアント
  └─ 案件（受注見込み・発注見込み・完了予定日・入金予定日・支払予定日）
       ├─ 見積（複数可）
       │    └─ 印刷仕様（複数可）→ 見積依頼メール
       ├─ 納品書（見積の明細から選んで作る）
       └─ 請求書（納品書を1つ以上まとめる）→ 送付 → 入金確認
```

に広げ、案件の数字を集計して **売上粗利管理表・入出金予定表・クライアントカルテ** を
自動生成する。

**要となるのは「案件」の導入。** 見積・納品・請求・入金・管理表のすべてが案件に
ぶら下がるため、ここを最初に作る。

---

## 2. 相談事項（※1〜※12）への提案

### ※1・※4 ネット印刷のスクショ取込と、マスタへの自動登録・URL

**提案: 「見積で使った価格」と「価格マスタ」を同じものにする。**

いまは価格マスタが独立した画面で、見積からは「マスタに登録済みのセル」を選ぶだけ。
これを、見積の明細追加パネルに「ネット印刷ページから取り込む」を足し、

1. URL入力 or スクショ選択 → AIが価格表を読み取る
2. **その場で価格マスタに登録**（URL・スクショの両方を保存）
3. 読み取った価格表から使うセルを選んで明細に入れる

とする。マスタのレコードには `source_url` と `screenshot_url` の列がすでにあるので、
スキーマ変更は最小で済む。

URLは持った方がよい。理由は2つ。
- 「URLから更新」で後日の価格改定を追える（機能はすでにある）
- クライアントカルテ（※12）の「一次情報」になる。問い合わせ時に見積番号と一緒に示せる

### ※2・※5 印刷仕様を入力するタイミングとUI

**提案: 「印刷仕様」を明細とは別の入力欄として持ち、依頼メールはそこから生成する。**

いまのメール生成は明細行から仕様を組み立てているが、順序が逆になっている。
印刷所に見積を依頼するのは**価格が分かる前**なので、明細（価格）より先に仕様が要る。

流れを次のようにする。

```
印刷仕様を入力 → 依頼メール生成・送信 → 見積書PDFが届く → 取込で明細化（A-1）
```

旧形式の見積が持っていた `print_type / size / paper_type / quantities / color_count /
desired_delivery_date` の列がそのまま「仕様」として使える。新形式の画面に
**「印刷仕様」タブ**を追加し、これらを入力する。

1つの見積で「チラシ」と「ラベル」のように複数の印刷物を扱うことがあるので、
仕様は**複数持てる**形にする（`print_specs` テーブル、見積1:N仕様）。
依頼メールは仕様ごとに生成できる。

これが入ると、旧形式の画面（仕様タブ・印刷費タブ）は役目を終えるので、
**旧形式UIを廃止して新形式に一本化**できる。

### ※3 過去見積からの検索範囲

**提案: 既定は同一クライアント内、「他のクライアントも含む」で広げられるようにする。**

追加印刷はほぼ同じクライアントの同じ商品なので、既定を絞った方がノイズが少ない。
一方で「同じ仕様のチラシを別のクライアントでも」という場合もあるので、切り替えは残す。

検索結果には **前回の原価・売価・入稿先（印刷所 or URL）・見積番号** を出し、
選ぶと明細に複製される。明細には「前回: CV-2607-001 から複製」の印を残す。

### ※6・※7 Webサイト制作・システム構築のスプレッドシートインポート

**※6 提案: 固定テンプレートのシートを用意し、それを取り込む。**

列は明細（`line_items`）と1対1にする:
`大カテゴリ / 名称 / 数量 / 単位 / 単価 / 原価 / 備考`

アプリから「テンプレートをダウンロード」でき、記入後に貼り付け（CSV）または
Googleスプレッドシートを指定して取り込む。Sheets連携は工程管理表の出力でも使うので、
仕組みを共用する。

**※7 は実物を見てから判断したい。** 項目が数十行になる場合、

- 見出し行でグループ分け（テキスト行はすでにある）
- グループごとの小計行
- PDFの改ページ

が必要になる見込み。**実際のWeb案件の見積スプレッドシートを1つ**見せていただければ、
現行フォーマットのままでよいか、長尺用の体裁を分けるかを判断できる。

### ※8 複数納品書を1枚の請求書にまとめる

**提案: 請求書は納品書を1つ以上持つ（1:N）。明細は納品書ごとに見出しを付けて並べる。**

```
【納品書 D-2609-001】9/10納品 チラシ印刷
  チラシ印刷費 ……… 1,000枚 …… ¥46,000
【納品書 D-2609-003】9/25納品 ラベル印刷
  パッケージラベル印刷費 … 3,000枚 …… ¥90,000
                                  小計 ¥136,000
                                  消費税 ¥13,600
                                  合計 ¥149,600
```

納品書単位の見出しがあると、先方が自分の受領記録と突き合わせやすい。

送付はアプリから（メール送信の仕組みはすでにある。PDFを添付して送る）。
郵送の場合はPDF出力。**電子印鑑は「郵送用」を選んだときだけ非表示**にする。

ステータス: `送付前`（既定）→ `送付済`→ `着金済`。入金予定日を過ぎても着金していない
ものは `未入金` として赤系で表示。

### ※9 MF会計との連携（CSV）

**提案: 請求書確定と入金確認の2つのタイミングで仕訳を作り、MFの取込形式でCSV出力する。**

| タイミング | 仕訳 |
|---|---|
| 請求書を送付済にした | 売掛金 / 売上高 |
| 着金を確認した | 普通預金 / 売掛金 |

**MF会計の仕訳インポートの列構成は、会計事務所に確認が必要**（勘定科目の名称や
税区分の表記が事務所の設定に依存するため）。サンプルCSVを1つもらえれば、
それに合わせて出力する。

### ※10 目標との差分表示

**提案: 月ごとに「目標・見込み・確定・実績」の4本を1つの表と1つのグラフにまとめる。**

| | 意味 | 元データ |
|---|---|---|
| 目標 | 売上目標・粗利目標・最低粗利目標 | システム設定で月ごとに登録 |
| 見込み | 受注見込み金額の合計 | 案件 |
| 確定 | 請求書を送付済にした金額 | 請求書 |
| 実績 | 着金済の金額 | 入金確認 |

差分は「目標まであと ¥X（達成率 Y%）」の形で、色は3段階。

- 達成 → 緑
- 80%以上 → 黄（この閾値と目標粗利率は設定で変更できるようにする。2026-09-16 回答）
- 80%未満 → 赤
- **粗利が最低粗利目標を下回った月は、達成率に関係なく赤で警告**

グラフは月別の棒（見込み・確定・実績を並べる）に、目標を横線で重ねる。
これ以上凝ると見なくなるので、まずはこの1表1グラフで運用してから足す。

目標は「期」でも見たいはずなので、**決算月**を設定に持ち、期の累計も出す。

### ※11 支払いスケジュールの決め方

**提案: 既定を「完了予定日の翌月末」にし、案件ごとに変更できるようにする。**

入金予定日の既定（完了予定日の翌月末）と同じ考え方で揃える。
印刷所の多くは月末締め翌月末払いなので、既定のままで大半は合う。

段階的に精度を上げる。

1. まず: 案件に `支払予定日` を持たせ、既定は完了予定日の翌月末。手で変更可
2. 次に: 印刷所情報に `支払サイト`（例: 月末締め翌月末 / 翌々月10日）を持たせ、
   案件で選んだ発注先から自動で決める
3. 将来: 仕入先からの請求書を登録したら、見込みを実額に置き換える

### ※12 クライアントカルテ

**提案: 4つのブロックで構成する。既存のスプレッドシートを見て項目を合わせる。**

| ブロック | 内容 | 元データ |
|---|---|---|
| 基本情報 | 社名・担当者・連絡先・住所・備考 | clients（すでにある） |
| 売上・粗利 | 月別・期別の推移（表＋グラフ）。一覧画面でも並べて見られる | 案件・請求書 |
| 案件・見積一覧 | 案件ごとの見積・納品・請求の状況 | 案件 |
| 印刷履歴 | 商品ごとに、入稿先（印刷所 or ネット印刷URL）・原価・一次情報（スクショ／URL） | 明細 ＋ 価格マスタ（※1の設計が効く） |

「原価と一次情報が見える」は※1でURLとスクショを明細から辿れるようにしておけば、
カルテ側は集めて並べるだけで済む。

**現行のスプレッドシートを見せていただければ、項目名や並びをそこに合わせる。**

### 電子印鑑

見積書・納品書・請求書のPDFに載せる。印影の画像を Supabase Storage に置き、
PDF生成時に重ねる。「郵送用」のときだけ外す（※8）。

---

## 3. 進め方（6ステップ）

依存関係の順に並べた。各ステップの終わりに動作確認をしてから次へ進む。

| ステップ | 内容 | なぜこの順か |
|---|---|---|
| **S1 案件** ✅ | 案件テーブル・作成画面・見積との紐づけ。既存16件の見積には案件を自動生成して紐づける。freee販売の794件の取込スクリプト。手順は `docs/v2-s1-setup.md` | すべての土台。ここが無いと納品・請求・管理表が作れない |
| **S2 見積フロー** | ✅ S2-1 値付けルール（原価売価計算表の再現。%はシステム設定で変更可）／ ✅ S2-2 印刷仕様タブ（※2・5。手順は `docs/v2-s2-setup.md`）／ ✅ S2-3 ネット印刷取込→マスタ自動登録（※1・4）／ ✅ S2-4 過去見積からの複製（※3）／ ✅ S2-5 旧形式→新形式の変換ボタン（全件変換後に旧UIのコードを削除） | 日常業務で毎回使う部分。案件の次に価値が大きい |
| **S3 納品書・請求書** ✅ | S3-1 納品書・会社情報・電子印鑑・PDF ／ S3-2 請求書（納品書をまとめる ※8・入金期日・税率別内訳・入金記録）／ S3-3 PDF添付のメール送付 ／ S3-4 freee請求書597件の取込。手順は `docs/v2-s3-setup.md` | 請求まで通って初めて「業務が一巡」する |
| **S4 入金・会計** ✅ | 銀行CSV（東邦・琉球）の取込と請求書との照合（金額・振込名義・フリガナ、学習あり）、MF会計向け仕訳CSV（※9、勘定科目は設定可）。手順は `docs/v2-s4-setup.md` | 請求書が無いと照合できないので S3 の後 |
| **S5 管理表・カルテ** ✅ | S5-1 売上粗利管理表（※10。目標は期ごとに設定、見込は請求済みを差し引く）／ S5-2 入出金予定表（※11）／ S5-3 クライアントカルテ（※12。入稿先の一次情報付き）。手順は `docs/v2-s5-setup.md` | すべてのデータの集計なので最後 |
| **S6 スプレッドシート連携** ✅ | S6-1 スプレッドシートからの明細取込（※6・7。見出し行の自動判定、見出し／小計行）／ S6-2 小計行と長尺印刷 ／ S6-3 案件の工程と Google スプレッドシートへの工程管理表出力・クライアント共有。手順は `docs/v2-s6-setup.md` | 独立性が高く、他と並行できる |

S6 は他と依存が薄いので、Web案件の見積が先に必要なら前倒しできる。

---

## 4. 着手前にいただきたいもの

| # | もの | 使う場所 |
|---|---|---|
| 1 | **クライアントカルテの現行スプレッドシート** | ※12 の項目合わせ |
| 2 | **Web/システム案件の見積スプレッドシート（実物1つ）** | ※7 の判断 |
| 3 | **MF会計の仕訳インポートCSVの形式**（会計事務所に確認） | ※9 |
| 4 | **銀行の入金明細CSV**（ヘッダー行と数行。金額は伏せて可） | S4 の照合ロジック |
| 5 | **電子印鑑の画像**（PNG、背景透過が望ましい） | S3 |
| 6 | **決算月**と、**目標の単位**（月ごとに設定するか、期の目標を月割りにするか） | ※10 |
| 7 | **現在使っている見積書・納品書・請求書の様式**（紙やPDF） | S3 の帳票レイアウト |

1・2・7 は S1 の間に見せていただければ十分。3・4 は S4 まで、5 は S3 まで。

---

## 5. 決めておきたいこと

- ~~**案件番号の採番**: 見積番号 `CV-YYMM-連番` と揃えて `P-YYMM-連番` のような形でよいか~~ → S1 で `P-YYMM-連番` を採用
- **納品書・請求書の番号**: 同様に `D-YYMM-連番` / `I-YYMM-連番` でよいか
- ~~**見積の「受注確度」「フェーズ」**は見積ではなく**案件**の属性に移す方が自然~~ → S1 で案件の属性に移した
- **消費税の扱い**: 現状は税率10%固定。→ **ごく稀に軽減税率あり**（2026-09-16 回答）。S3 で明細ごとに税率（10%/8%）を選べるようにし、帳票の内訳も税率別に出す

---

## 6. いただいた資料の所見（2026-09-16 受領・全7点）

資料の実物は個人情報・取引情報を含むため、リポジトリには入れない。
取込用のスクリプトだけをコミットし、元データは実行時に手元で指定する（Base44移行と同じ方式）。

### 会社の基本情報（帳票・管理表から）

| 項目 | 値 |
|---|---|
| 決算月 | **9月**（CV15期 = 2025年10月〜2026年9月） |
| 適格請求書発行事業者 登録番号 | T7011001094049 |
| 振込先 | 東邦銀行 郡山営業部（普通）2312454 ／ 琉球銀行 本店営業部（普通）1353283 |
| 住所 | ［福島］郡山市安積荒井三丁目497-B号 cv-studio ／［沖縄］那覇市久米2-9-11 Abc久米ビル3階 |
| 消費税 | 10%のみ（帳票に軽減税率・非課税の記載なし） |

### 案件（freee販売のエクスポート 794件・2024-10〜2026-09）

引き継ぐ列: 案件登録日 / 顧客名称 / 案件名称 / 受注確度 / フェーズ / 受注見込 / 発注見込 /
仕入合計 / その他費用 / 完了予定日 / 社内メモ / 受注合計 / 発注合計 / 粗利（見込・実績）

| 区分 | 値（件数） |
|---|---|
| 受注確度 | A（593）／ C（126）／ **A（定期売上）**（32）／ **要注意（A）**（28） |
| フェーズ | 引き合い（604）／ 着手中（119）／ 未着手（62）／ 受注済（2） |

- **受注確度・フェーズは案件の属性**として持つ（現在は見積にあるが、freee販売と同じ位置に移す）
- 「定期売上」は独立した概念。管理表でも「定期売上＋DRAW UP事業」を別枠で集計している
- 顧客名称に `【freee送付】`『【定期あり／freee送付】』などの**運用タグが接頭辞**として付いている。
  取込時にタグを剥がしてクライアントマスタと突合し、タグは別属性（送付方法・定期の有無）に移す

### 請求書（freeeの請求書一覧エクスポート 598件・2024-10〜）

列: 請求No. / 請求日 / 請求先名称 / 請求書件名 / 請求金額(税抜) / 送付ステータス

- **これはMF会計の仕訳形式ではない。** 会計事務所がこれをそのまま取り込んでいるのか要確認（⑦）
- 件名の先頭に `【入金済】` を手で付けて入金管理している → 新システムでは着金ステータスに置き換わる
- 番号 `INV-0000000655` 形式。新システムの請求書番号は別体系にし、取込分は元番号を保持

### 帳票レイアウト（freee生成の見積書・納品書・請求書）

3帳票とも同じ骨格: 宛名（御中）／件名／自社情報（2拠点・電話FAX）／小計・消費税・合計の帯／
明細表（摘要・数量・単価・明細金額）／内訳（10%対象・消費税）／備考。

| 帳票 | 固有の項目 |
|---|---|
| 見積書 | 見積日・見積書番号・有効期限（6ヶ月） |
| 納品書 | 納品日・納品書番号。自社情報は福島のみ |
| 請求書 | 請求日・請求書番号・**登録番号**・**取引日の列**・**入金期日**・**振込先2行** |

明細枠は20行固定。Web案件（60〜80行）は収まらないため、長尺用の体裁が必要（下記）。

### 値付けルール（「デザイン制作原価売価計算表」より）

案件ごとに1シートで原価・売価・粗利を計算している。**アプリの明細計算に組み込む候補**（⑧で確認）:

| 項目 | ルール |
|---|---|
| CV割引 | デザイン費の 5% / 10% / 20%、Draw up 事業割引 25% |
| コンセプト設計費 | 印刷費を除く合計の **20%**（百の位を切り上げ） |
| 校正費 | デザイン費合計の **7%**（百の位を切り上げ）。対象: チラシ・リーフレット・パンフレット・Web |
| 印刷費 掛け率 | パッケージ **1.3** ／ パッケージ以外 **1.35**（現在の `MARKUP_RATES` と一致） |
| 外注 | 構築費 0.75 ／ 撮影費 0.6（仕入に対する比率） |
| 粗利率の前提 | プロデュース・企画・コンサル 100% ／ モデル 33.3% |

さらに参考価格表（ブランドロゴ 150,000〜、チラシ片面 70,000〜 両面 100,000〜、
パンフレット 100,000〜、リーフレット 80,000〜 など、提案数・校正回数付き）が同居している。
→ デザイン費マスタの拡充に使える。

### Web/システム案件の見積（3件）

- 60〜80行。**3階層**（大項目 → ページ → 下層ページ）で、大項目ごとに小計
- 「コンセプト設計（全体予算の5%）」「ディレクション費（全体予算の15%）」のように
  **他の行の合計から比率で決まる行**がある
- 単位が多様（ページ・式・名・回・件・社・ヶ月・種）
- 現行の見積書PDF（20行枠）には収まらない → 長尺用: 見出し行・小計行・改ページ対応

インポート用テンプレートの列: `階層 / 大カテゴリ / 名称 / 数量 / 単位 / 単価 / 原価 / 備考`

### 銀行の入金明細（2行・形式が異なる）

| 銀行 | 形式 |
|---|---|
| 東邦銀行 | 1行目がヘッダ（`1,日付,支店,種別,口座,名義`）、以降 `2,"2026- 4- 3","振込名義","","\33,000","\342,799"`。日付の月日はスペース埋め、金額に `\` とカンマ |
| 琉球銀行 | 1行目がヘッダ（銀行,支店,種別,口座,開始日,終了日,出力日時）、以降 `ID,日付,出金,入金,摘要,残高` |

- 振込名義は**半角カナ・法人格が略記**（`(ｶ)`, `ｶ)`, `ﾄｸﾋ)`, `.ｶ.`）。請求先との照合には正規化が要る
- 両形式に対応し、取込時に銀行を自動判別する

### 売上粗利管理表（画面より。xlsx は⑪で任意）

期（10月〜9月）× 月の表。4段構成:

| 段 | 内容 |
|---|---|
| 目標 | 売上 / 仕入 / 目標粗利（ジャンプ）/ 目標粗利 / 必達粗利。**月ごとに同額（期目標の月割り）** |
| 着地見込 | 売上（案件見込 A）/ 売上（要注意 A）/ 発注見込 A / 発注見込（要注意）→ 粗利・粗利率 |
| 実績 | 売上（請求ベース）/ 調達（仕入・銀行振込ベース）/ その他原価 → 粗利・粗利率 |
| 計 | 上記合算。粗利率に **「平均粗利80%を必達」** の注記 |

各段に **必要売上・必要粗利（目標までの残り）** を3段階（ジャンプ／目標／必達）で表示。
別枠で「定期売上＋DRAW UP事業」を月別に集計。
凡例: 繁忙期／予算達成／予算未達 の色分け。「売上日と請求日は必ず一緒にする」の運用ルールあり。

→ ※10 の提案（1表1グラフ）をこの4段構成に合わせて具体化する。

### 未受領・未回答

| | 状況 | 影響 |
|---|---|---|
| 電子印鑑（ファイル） | システム設定 → 会社情報（帳票用）から本人がアップロードする形にした | 対応済 |
| ⑦ MF会計の取込形式 | 一般的な MF の仕訳インポート形式で出力（2026-09-20 回答）。勘定科目・税区分は画面で変更可 | 対応済 |
| ⑧ 原価売価計算表の現用有無 | **現用中**（2026-09-16 回答）。CV割引・コンセプト設計費・校正費などの%は**設定で変更できるように**する | S2-1 で実装済み（`src/lib/pricing.js`、システム設定「値付けルール」、見積書タブ「自動計算行」「外注の仕入額から売価」） |
| ⑨ 角印の要否 | 未回答。S3 ではシステム設定でアップロードした印影1種を帳票に載せる形にした | 必要なら追加 |
| ⑩ 定期売上の自動生成 | **毎月自動生成で確定**（2026-09-16 回答） | S1 で実装済み（`0005_recurring_projects.sql`・ひな形画面・Vercel cron） |


---

## 7. フィードバック対応（2026-09-23）

「全体整理v2」の 38 項目＋4 項目への対応は `docs/feedback-2026-09.md` を参照。
セットアップは `supabase/migrations/0011_feedback_1.sql` と `0012_tax_inclusive.sql` の実行。

独自ドメイン（cv-ax.jp）の設定手順は `docs/domain-setup.md`。
